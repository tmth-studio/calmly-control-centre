{
  "name": "calmly-control-centre",
  "version": "1.0.0",
  "description": "Calmly Control Centre — project hub for Calmly venture artifacts",
  "scripts": {
    "start": "npx serve -s . -l ${PORT:-3000}"
  },
  "engines": {
    "node": ">=18"
  }
}
